// The `int` type is 64-bit signed integers (same as `long` in Java) and supports usuall arithmetic operators: `+ - / %`. 
// Operator precedenc is same as C. Integer overflow is a runtime error in Ballerina.