name: Ballerina Distribution Build

on:
  push:
    branches:
      - master
      - ballerina-1.1.x
  pull_request:
    branches:
      - master
      - ballerina-1.1.x

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build Ballerina Distribution
        env:
          packagePAT: ${{ secrets.READ_PACKAGE_PAT }}
          packageUser: ${{ secrets.PUBLISH_USER }}
          nexus3User: ${{ secrets.NEXUS3_USERNAME }}
          nexus3Password: ${{ secrets.NEXUS3_PASSWORD }}
        run: mvn -s settings.xml clean install -B -V
