# In the directory, which contains the `.bal` file, create a directory named `files`,
# and add a CSV file named `sample.csv` in it.
# In the directory, which contains the `.bal` file, create a directory named `file`,
# and add an image file named `ballerina.jpg` in it.
# To run this sample, navigate to the directory that contains the
# `.bal` file, and execute the `bal run` command below.
bal run json_csv.bal
json record successfully transformed to a csv, file could be found in ./files/sample.csv
