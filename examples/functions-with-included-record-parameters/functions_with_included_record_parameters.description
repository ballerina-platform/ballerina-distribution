// A Ballerina function can have included record parameters.
// The included record parameter must be of the `record` type and it cannot have default values.
// A function with included record parameters can be invoked by providing values for the fields of the included
// record parameters as named arguments using the field’s names by declaring them as parameters.
// Arguments for included record parameters can also be passed as positional arguments.
