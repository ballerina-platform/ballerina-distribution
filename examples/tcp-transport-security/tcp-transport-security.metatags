description: BBE on how the Ballerina TCP client and TCP listener listener can be configured to use 1-way SSL connection (i.e. the server is verified by the client)
keywords: ballerina, ballerina by example, bbe, ssl, tls, tcp client, tcp listener
