// `decimal` type is the third numeric type. It works like `int` and `float` types.
// It is a separate basic type and counts as `anydata` type.
// Implicit conversions between `decimal` and other numeric types is not allowed.
// It can represent `decimal` fractions exactly and it preserves precision.
// `decimal` type does not include `infinity`, `NaN` or negative zero.
// It supports floating-point precision to 34 decimal digits.
