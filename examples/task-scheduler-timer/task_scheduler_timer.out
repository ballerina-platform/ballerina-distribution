// A scheduler can have any number of services attached to it. All the attached services
// will run when the scheduler triggers them.
// A `CounterContext` record is attached to the Scheduler when attaching the service.
// It is then passed into the resource functions of the service.
# To run this sample, navigate to the directory that contains the
# `.bal` file, and execute the `bal run` command below.
bal run task_scheduler_timer.bal
MyCounter: 1
MyCounter: 2
MyCounter: 3
MyCounter: 4
MyCounter: 5
End.