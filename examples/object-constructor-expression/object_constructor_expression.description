// Values of object types can be defined in-line using object constructor expressions.
// When the object type implementation does not need to be re-used,
// an object constructor expression can be used instead of the `class` and a `new` expression.
