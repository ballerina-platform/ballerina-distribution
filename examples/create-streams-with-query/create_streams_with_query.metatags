description: Create a stream of values, load values lazily, save resources by loading values when needed,
             stream construct type, handling the termination value of a stream
keywords: ballerina, ballerina by example, bbe, stream, lazy, value, next, while
