# As a prerequisite, navigate to the directory that contains the
# `tcp_listener.bal` file and use the `bal run` command below.
bal run tcp_listener.bal

# To run the client, navigate to the directory that contains the
# `.bal` file and use the `bal run` command below.
bal run tcp_client.bal

# Print the response that is obtained from the server.
Recived: Hello Ballerina Echo from client
