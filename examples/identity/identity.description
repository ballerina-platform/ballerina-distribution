// `===` and `!==` operators test for identity. Identical mutable basic types means that they are stored at the same address.
// `==` and `!=` are not defined for objects. `-0.0` and `+0.0` are equal but not identical.
