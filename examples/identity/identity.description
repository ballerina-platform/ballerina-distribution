// `===` and `!==` operators test for identity. Identical for mutable basic types means stored at the same address.
// `==` and `!=` are not defined for objects. `-0.0` and `+0.0` are equal but not identical.
