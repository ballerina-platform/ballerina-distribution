// `===` and `!==` operators test for identity. The identity is identical for mutable basic types means they are stored at the same address.
// `==` and `!=` are not defined for objects. `-0.0` and `+0.0` are equal but not identical.
