# Send a query to the GraphQL endpoint using a cURL command.
# The query used: mutation { addPost(newPost: { author: "Sam", content: "Hello" }) { id } }
$ curl -X POST -H "Content-type: application/json" -d '{ "query": "mutation { addPost(newPost: { author: \"Sam\", content: \"Hello\" }) { id } }" }' 'http://localhost:4000/graphql'
{"data":{"addPost":{"id":0}}}
