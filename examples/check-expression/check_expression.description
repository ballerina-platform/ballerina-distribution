// `check E` is used with an expression `E` that might result in an `error`.
// If `E` results in an `error`, then `check` makes the function return that `error`
// immediately.
// Type of `check E` does not include `error`.
// The control flow remains explicit.
