# Work with Excel tables

The Ballerina `xlsx` library supports Excel tables, which are named, structured ranges within a sheet. A table can be created directly from an array of records and, since table names are unique across a workbook, read back by name without specifying the sheet.

::: code xlsx_tables.bal :::

::: out xlsx_tables.out :::
