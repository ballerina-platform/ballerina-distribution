// Ballerina allows defining custom error types to match
// the errors being modeled. User defined error types in Ballerina could have optional
// type parameter specifying error detail type,
// which should be subtype of `map<anydata|readonly>`.
//
// When detail type is absent from error type definition error type
// defaults to `map<anydata|readonly>`.

